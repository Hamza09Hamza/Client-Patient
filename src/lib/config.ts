/** Single source of truth for the clinic's display name — change once, everywhere updates. */
export const CLINIC_NAME = "Clinique Amina";

/** Placeholder — replace with the clinic's real phone number. Shown to patients when
 * online password-reset requests are disabled, see src/lib/feature-flags.ts. */
export const CLINIC_PHONE = "+213 00 00 00 00";
